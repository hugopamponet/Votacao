spring:
  datasource:
    url: jdbc:mysql://localhost:3306/INFORME_SEU_BANCO?useSSL=false&serverTimezone=America/Sao_Paulo
    username: root
    password: *****
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect